
const loginpage = ({ children }: { children: React.ReactNode }) => {
    return (
        <div className={`h-full flex items-center justify-center custom-gradient `}>
            {children}
        </div>
    )
}

export default loginpage